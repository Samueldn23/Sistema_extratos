[build]
builder = "nixpacks"

[deploy]
startCommand = "node src/servidor.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/api/status"
timeoutSeconds = 10
