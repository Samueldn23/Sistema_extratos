[build]
builder = "nixpacks"

[deploy]
startCommand = "node api/dev-local.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/api/status"
timeoutSeconds = 10
